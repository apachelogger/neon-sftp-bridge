description "Neon SFTP Bridge to depot.kde.org"
author "me"

start on (filesystem and net-device-up IFACE!=lo)
stop on runlevel [!2345]

setuid neon-sftp-bridge
setgid neon-sftp-bridge

chdir /home/neon-sftp-bridge
env HOME=/home/neon-sftp-bridge
env PORT=9191

respawn

script
    exec /home/neon-sftp-bridge/bin/neon-sftp-bridge
end script
